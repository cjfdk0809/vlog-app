# ams_ax — 품의서 자동생성기

키움F&I 자산관리부 표준양식(HWP) 기반 품의서 초안 생성 도구.

## 구조
- `app.py` — Streamlit 화면
- `ams_ax/hwp/` — HWP 5.0 바이너리 읽기·쓰기 (records / reader / writer)
- `ams_ax/data/` — 실사파일 추출(workpaper), 값 가공(filters)
- `ams_ax/mapping.py` — 셀 매핑표 로더
- `forms/` — 표준양식 .hwp
- `mapping/mapping.xlsx` — 셀 매핑 설정 (코드 아님, 이 파일만 고치면 됨)

## 절대 어기면 안 되는 것 — HWP 안전 규칙
어기면 한글이 "파일이 손상되었습니다" 또는 "변조되었을 가능성"으로 거부한다.
`ams_ax/hwp/writer.py` 에 구현돼 있으며, 수정하더라도 반드시 유지할 것.

1. PARA_HEADER 글자수의 최상위 플래그 비트 보존
2. 문단 끝 제어문자(0x0D) 보존
3. 빈 문단(길이 0) 생성 금지
4. 셀의 문단 개수 변경 금지
5. 텍스트 변경 시 줄나눔(LINE_SEG)을 1줄로 정규화

`olefile`은 원본과 같은 크기로만 스트림을 덮어쓸 수 있어,
재압축 결과를 원본 크기에 맞춰 0으로 패딩한다.

## 업무 규칙
- OPB = Loan 시트 `대위변제원금차감` 합계 → 없으면 `OPB` 라벨값 → 없으면 `대출원금잔액` 합계
- 자료 미확보 항목은 **빈칸** 출력. 양식의 예시값이 남으면 실데이터로 오인된다
- 표 행 추가·삭제는 하지 않는다 (규칙 4)
- 프로그램이 문장을 새로 쓰거나 항목을 신설·삭제하지 않는다
- 이 도구는 초안 생성기다. 검토·사례 교체·시점 갱신은 담당자가 한다

## CI
- 네이비 `#011F8E` (공식 로고 추출값)
- 마젠타 `#E6007E`
- 폰트 맑은 고딕, 흰 배경 + 네이비 텍스트 기조
- 로고는 `assets/logo.png` 공식 파일만 사용. **텍스트로 대체 생성 금지**

## 현재 상태
- 매핑 완료: 공매 46칸(자동38/수동2/빈칸6), 방어입찰 194칸(자동108/수동32/빈칸54)
- 미착수: 속행, 매각기일변경, 입찰이행계약, 채무인수계약
- 방어입찰 수동 입력 32칸이 과다. 유입검토·회수예상액 산식 확정되면 축소 예정

## 실행


## 응답 규칙
- 한국어로 답한다. 실무 용어는 그대로 쓴다
- 사용자는 비개발자다. 코드 변경 시 "어느 파일의 어디를 왜" 를 먼저 밝힌다
- 불확실한 서식·데이터 구조는 임의로 가정하지 말고 질문한다
